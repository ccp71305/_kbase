## This file lists all the review comments made for this PR. 

- Please go through each comment. Some comments may make sense and some may not and that is ok. 
- The reviewer may not have understood the context of the code changes or may have missed some details. 
- You should understand the intent of the comment and try to address it in the best possible way.
- You may have to explain how certain annotations work in the AWS SDK V2 and how the cloud-sdk-api and cloud-sdk-aws implementations work together to achieve the desired functionality.
- You may have to explain how the new annotations work and where and how they are used. Some DynamoDb annotations in AWS SDK V2 are allowed only in the method level and not in the attribute level for example. 
- You may refer to an existing Integration test that verifies the functionality of the new annotations and how they work together to achieve the desired functionality and document that in your response by pointing to the relevant test class and test method that verifies the functionality of the new annotations.
- You may need to add new tests if missing for any gaps or missing annotations or functionality.
- if the review comment alludes to a real gap or potential issue or bug you should write a test that reproduces the issue and then fix the issue and verify that the test passes and document the same in your response to the review comment.

## The review comments in this PR are listed below

### booking/src/main/java/com/inttra/mercury/booking/inbound/confirmation/ConfirmPendingMessage.java

- How will the impact of the removed annotations be handled?

### booking/src/main/java/com/inttra/mercury/booking/model/BookingDetail.java

- Why are the hashkey annotations removed? Is this handled in an alternative way? Referring to removed annotations @DynamoDBRangeKey and @DynamoDBAutoGeneratedKey for old version getSortKey() which is now replaced by getSequenceNumber()

- All the previously relevant annotations seem to be removed? Is this handled in a different way?

- Referring to removal of @DynamoDBIndexHashKey and @DynamoDBIndexRangeKey annotations on multiple index fields

- Why are @DynamoDBIgnore annotations removed ?

- Concerns on removal of implements DynamoHashAndSortKey<String, String> for the BookingDetail class and how the hash and sort keys are now defined and handled in the new implementation.

- @DynamoDBStream annotation is removed and this seems to be valid and corresponding cloud-sdk-api annotation is missing. 
Check this in network or auth or booking-bridge modules and see usage and we need to add this to the model

### booking/src/main/java/com/inttra/mercury/booking/externalwrapper/exception/UnrecoverableAWSException.java

- Concern on the manner the exception is now handled and will it have any impact ?

### booking/src/main/java/com/inttra/mercury/booking/outbound/model/EnrichedAttributes.java

- Removal of @DynamoDBIgnore on certain methods have been flagged. Why are these removed and how is this handled in the new implementation?
e.g. getLocationAdditionalInfoForLocation() method have @DynamoDBIgnore annotations removed. Why are these removed and how is this handled in the new implementation? Are these now being persisted in the database or are they being handled in a different way?

### booking/src/main/java/com/inttra/mercury/booking/dynamodb/BookingStateConverter.java

- Why remove the dynamo annotations? will the json be stored fine even if we remove this?
Test if there is a proper integration test that verifies the functionality of this converter and if not add a new test to verify the functionality of this converter and how it works with the new annotations and document the same in your response to this review comment by pointing to the relevant test class and test method that verifies the functionality of this converter. Make sure DynamoDb persistence is happening fine with the new annotations and the converter is working as expected.

### booking/src/main/java/com/inttra/mercury/booking/model/DateToEpochSecond.java

- same concern as above 

### booking/src/main/java/com/inttra/mercury/booking/model/CancelMessage.java

- Why remove the dynamo annotations? will the json be stored fine even if we remove this?
Test if there is a proper integration test that verifies the functionality of this model and if not add a new test to verify the functionality of this model and how it works with the new annotations and document the same in your response to this review comment by pointing to the relevant test class and test method that verifies the functionality of this model. Make sure DynamoDb persistence is happening fine with the new annotations and the model is working as expected.

### booking/src/main/java/com/inttra/mercury/booking/model/BookingRequestContract.java

- Why remove the dynamo annotations? will the json be stored fine even if we remove this?
Test if there is a proper integration test that verifies the functionality of this model and if not add a new test to verify the functionality of this model and how it works with the new annotations and document the same in your response to this review comment by pointing to the relevant test class and test method that verifies the functionality of this model. Make sure DynamoDb persistence is happening fine with the new annotations and the model is working as expected.

- The removal of @DynamoDBTypeConverted(converter = BookingStateConverter.class) annotation on the bookingState field is flagged as "Will the conversion work?". 
Why is this removed and how is this handled in the new implementation? Is the converter still being used and working as expected with the new annotations? Please verify if there is a proper integration test that verifies the functionality of this converter with the new annotations and if not add a new test to verify the functionality of this converter with the new annotations and document the same in your response to this review comment by pointing to the relevant test class and test method that verifies the functionality of this converter with the new annotations. Make sure DynamoDb persistence is happening fine with the new annotations and the converter is working as expected.

- The removal of @DynamoDBDocument annotation on the BookingRequestContract class is flagged as "Why remove this?"

### booking/src/main/java/com/inttra/mercury/booking/dynamodb/S3Archive.java

- The removal of this class is questioned. Was this class being used anywhere ? 

### booking/src/main/java/com/inttra/mercury/booking/dynamodb/SpotRatesConverter.java

- The removal of implements DynamoDBTypeConverter<String, SpotRates> is flagged. Why is this removed and how is this handled in the new implementation? Is the converter still being used and working as expected with the new annotations? Please verify if there is a proper integration test that verifies the functionality of this converter with the new annotations and if not add a new test to verify the functionality of this converter with the new annotations and document the same in your response to this review comment by pointing to the relevant test class and test method that verifies the functionality of this converter with the new annotations. Make sure DynamoDb persistence is happening fine with the new annotations and the converter is working as expected.

### booking/src/main/java/com/inttra/mercury/booking/dynamodb/CreateTables.java

- create tables class for running create tables is being deleted in your PR. There could be upcoming scenarios where we need to create new tables for booking. the standard way right now is to have it created via this command when the application starts up. I do not think deleting this is a good idea.
The reviewer clearly does not understand how the new cloud-sdk-aws implementation for the DynamoDbAdminCommand works. Please explain how the new cloud-sdk-aws implementation for the DynamoDbAdminCommand works and how it handles the creation of tables and how it is different from the old implementation. Please also explain how we can create new tables for booking in the future with the new implementation and how it is better than the old implementation. Please refer to the relevant code files and documentation in your response to this review comment to provide a clear explanation of the new implementation and its benefits over the old implementation.
